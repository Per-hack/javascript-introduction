function doubleThemMutable (list){
    for (var i = 0; i< list.length; i++){
        list [i] = list [i] * 2;
    }
}

var arr = [3, 4, 5];
doubleThemMutable (arr);

arr;        // [6.8.10]

{
    var newList = [];
    for (var i = 0; i< list.length; i++){
        newList [i] = list [i] * 2;
    }
    return newList
}

var arr = [3, 4, 5];
var arr2 = 
doubleThemMutable (arr);

arr;        // [6.8.10]

